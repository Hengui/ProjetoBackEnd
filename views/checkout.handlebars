<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Checkout</title>
</head>
<body>
    <h1>Checkout</h1>
    <h2>Resumo do Pedido</h2>
    <ul id="checkoutList">
        {{#each cart.products}}
            <li>{{this.title}} - R$ {{this.price}}</li>
        {{/each}}
    </ul>
    <h3>Total: R$ {{cart.totalPrice}}</h3>
    <form action="/checkout" method="POST">
        <button type="submit">Finalizar Compra</button>
    </form>
</body>
</html>
